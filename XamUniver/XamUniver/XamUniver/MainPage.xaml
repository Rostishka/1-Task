<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XamUniver"
             Padding="10, 10, 10, 10"
             BackgroundColor="Aquamarine"
             Title="RostikBoss"
             x:Class="XamUniver.MainPage">
    <ContentPage.Resources>

    </ContentPage.Resources>

    <ContentPage.Content >
        <StackLayout Padding="20">
            <Label x:Name="Rosdt"
                Text="Welcome to Xamarin Forms!" 
                VerticalOptions="Start" 
                TextColor="{StaticResource LogInPageTextColor}">
                <Label.FontSize>
                    Large
                </Label.FontSize>
            </Label>
            <Label Text="{Binding FullName}"></Label>
            <Button Text="Some Button"
                    BorderColor="{StaticResource LogInPageTextColor}" 
                    VerticalOptions="Start"
                    Clicked="OnButtonClicked"
                    />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>